# Launch Roadmap Engine

`LaunchRoadmapEngine.generate(input)` turns execution initiatives into staged
launch buckets from today through twelve months. Each task preserves initiative
dependencies, KPI fields, evidence requirements, and planning notes.

The roadmap also carries weekly, monthly, quarterly, KPI, assumption, and pivot
review cycles forward from the execution plan.
