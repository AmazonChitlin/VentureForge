# Strategy Execution Engine

`StrategyExecutionEngine.buildExecutionPlan(input)` converts available
diagnosis into six execution stages. It always includes fifteen scoped
initiatives with dependencies, KPI fields, evidence gates, and planning-range
estimates.

Missing diagnosis remains visible. The engine reinforces the core operating
rule: diagnose before committing capital or capacity.
